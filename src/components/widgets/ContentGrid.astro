---
import ImagesGrid from './ImagesGrid.astro';
import type { ContentGridType as Props } from '~/types';

const {
  contentArray = [],
  bg = await Astro.slots.render('bg'),
} = Astro.props;
---


{
  contentArray && (
    <div class="text-center items-center">
      {contentArray.map((content) => 
        <ImagesGrid title={content.title} subTitle={content.subTitle}  text={content.text} callToAction={content.callToAction} images={content.images} bg={bg}/>
      )}
    </div>
  )
}
